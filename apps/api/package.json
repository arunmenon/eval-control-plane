{
  "name": "evaluator-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "ingest:benchmarks": "ts-node-dev src/ingestBenchmarks.ts",
    "seed:demo": "ts-node-dev src/seedDemo.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "fastify": "^4.25.0",
    "fastify-plugin": "^4.5.0",
    "parquets": "*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
